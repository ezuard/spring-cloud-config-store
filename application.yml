server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ezuard/spring-cloud-config-store/primary-config-store
          search-paths:
           - '*service'



